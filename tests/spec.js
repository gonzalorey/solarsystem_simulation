var assert = require('chai').assert;

require('./server.js');
require('./backend.js');
require('./utils.js');

// TODO: add the DB ones (but I'm too lazy to do it yet, as I would need to do an entire class...)
// require('./db.js');